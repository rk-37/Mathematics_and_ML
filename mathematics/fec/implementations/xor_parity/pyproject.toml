[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "xor-parity"
requires-python = ">=3.9"

[tool.maturin]
module-name = "xor_parity"
features = ["pyo3/extension-module"]
