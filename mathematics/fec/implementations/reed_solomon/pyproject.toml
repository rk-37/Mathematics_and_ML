[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "reed-solomon"
requires-python = ">=3.9"

[tool.maturin]
module-name = "reed_solomon"
features = ["pyo3/extension-module"]
